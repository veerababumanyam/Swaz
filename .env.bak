# Swaz Data Recovery Labs - Production Environment Configuration
# =================================================================

# Application Configuration
NODE_ENV=production
PORT=8080
DOMAIN=swazdatarecovery.com
SSL_EMAIL=manyamprasad@gmail.com

# Google Gemini AI Configuration
GEMINI_API_KEY=AIzaSyCbyGCWlBo7FgN2bshNhWi8PHOmK1484qE

# Server Configuration
SIGNALING_SERVER_PORT=443
WEBSOCKET_PROTOCOL=wss
HTTP_PROTOCOL=https

# Security Configuration
CORS_ORIGIN=https://swazdatarecovery.com
ALLOWED_HOSTS=swazdatarecovery.com,www.swazdatarecovery.com

# Nginx Configuration
NGINX_SSL_PATH=/etc/nginx/ssl
NGINX_CONFIG_PATH=/etc/nginx/sites-available

# Process Management
PM2_APP_NAME=swaz-data-recovery
PM2_INSTANCES=1

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/swaz-data-recovery/app.log

# Database (if needed in future)
# DATABASE_URL=
# REDIS_URL=

# Email Configuration (for contact forms)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# FROM_EMAIL=noreply@swazdatarecovery.com

# Monitoring (optional)
# SENTRY_DSN=
# GOOGLE_ANALYTICS_ID=


GEMINI_API_KEY=AIzaSyCbyGCWlBo7FgN2bshNhWi8PHOmK1484qE

# Outbound mail credentials (DO NOT COMMIT THIS FILE)
# User provided:
Email ID: info@swazdatarecovery.com
Password: Lmno-5678-pqrs-tuvw
Encryption: SSL
Port: 993  (Note: 993 is IMAP over SSL for receiving, not SMTP for sending.)

# We configure SMTP for sending. If your provider uses a unified host, set below.
# Common secure SMTP ports:
#  - 465 : Implicit SSL (secure=true)
#  - 587 : STARTTLS (secure=false, then upgraded)

MAIL_FROM=info@swazdatarecovery.com
MAIL_SMTP_USER=info@swazdatarecovery.com
MAIL_SMTP_PASS=Lmno-5678-pqrs-tuvw

# Set your actual SMTP host (examples: smtp.zoho.com, smtp.gmail.com, mail.privateemail.com)
MAIL_SMTP_HOST=smtp.hostinger.com

# Choose one style; defaulting to implicit SSL 465 since "Encryption: SSL" was specified
MAIL_SMTP_PORT=465
MAIL_SMTP_SECURE=true

# IMAP (inbound mail) configuration (optional). 993 implies implicit SSL.
# Use the same user/pass unless provider gives a separate one.
MAIL_IMAP_HOST=imap.hostinger.com
MAIL_IMAP_PORT=993
MAIL_IMAP_SECURE=true
MAIL_IMAP_USER=info@swazdatarecovery.com
MAIL_IMAP_PASS=Lmno-5678-pqrs-tuvw